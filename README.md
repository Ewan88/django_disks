
Requires Python
```
python3 --version

```
Setup Environment
```
python3 -m venv .env
source .env/bin/activate

pip3 install django
python -m django --version
```

Start Server
```
cd django_disks_project
python manage.py runserver
```
